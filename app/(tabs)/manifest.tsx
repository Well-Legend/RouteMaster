/**
 * 排單王 (RouteMaster) - 貨單頁籤入口 (列表視圖)
 */

import React from 'react';
import { DashboardScreen } from '../../src/features/dashboard';

export default function ManifestTab() {
    return <DashboardScreen viewMode="list" />;
}
