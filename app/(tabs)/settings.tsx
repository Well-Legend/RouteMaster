/**
 * 排單王 (RouteMaster) - 設定頁籤入口
 */

import React from 'react';
import { SettingsScreen } from '../../src/features/settings';

export default function SettingsTab() {
    return <SettingsScreen />;
}
